#include<iostream>
using namespace std;
int main()
{
    int n,m;
    cin>>n>>m;
    int arr[n];
    int freq[m=1];
    for(int i=0;i<n;i++)
    {
        cin>>arr[i];
        if(arr[i]==)
    }
    for(int i=1;i<=m;i++)
    {
        int count=0;
        for(int j=0;j<n;j++)
        {
            if(arr[j]==i)
            {
                count++;
            }
        }
        cout<<count<<endl;
    }
}